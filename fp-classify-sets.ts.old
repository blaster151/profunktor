// fp-classify-sets.ts
// Pipeline: derive model structures on Set from WFS and weak equivalences.

import { ALL_WFS, WFS } from "./fp-wfs-set";
import { ALL_MODEL_STRUCTURES, ModelStructure } from "./fp-model-sets";

export function classifySets(): ModelStructure[] {
  // Stub: in future, derive from lifting problems + WFS
  return ALL_MODEL_STRUCTURES;
}

export function listWFS(): WFS[] {
  return ALL_WFS;
}
